<table class='table table-hover table-inverse'>
  <thead>
    <tr>
      <th>photo</th>
      <th>user</th>
      <th>投稿タイトル</th>
      <th>コメント</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% @post_comments.each do |post_comment| %>
      <tr>
        <td>
          <%= post_comment.post.user.name %>
        </td>
        <td>
          <%= post_comment.user.name %>
        </td>
        <td>
          <%= post_comment.post.place %>
        </td>
        <td>
          <%= post_comment.comment %>
        </td>
        <td>
        <div class="mt-3 text-center">
          <div>
            <%= link_to "削除", admin_post_comment_path(post_comment), method: :delete, class: "btn btn-danger pull-right" %>
            <!--<#%= link_to "不適切な投稿の削除", admin_post_comments_path(@post_comment), method: :delete, class: "btn btn-danger", "data-confirm" => "不適切な投稿として削除しますか？" %>-->
          </div>
        </div>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>